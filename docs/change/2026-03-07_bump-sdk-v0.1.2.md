# 2026-03-07 bump SDK v0.1.2（MetricsNode 依赖升级）

## 背景 / 目标

- 背景：SDK 发布 `v0.1.2`（await 对 VarStore `MajorCmd` 响应的兼容路径）。
- 目标：MetricsNode 侧统一依赖基线到 `myflowhub-sdk v0.1.2`，保持与 Server/SubProto 的协议变更一致，减少联调版本分叉。

## 具体变更

- `go.mod`：`github.com/yttydcs/myflowhub-sdk v0.1.0 -> v0.1.2`
- `go.sum`：随依赖求解更新

## 测试与验证

- `GOWORK=off go mod tidy`（已执行）
- `GOWORK=off go test ./...`（已执行，通过）

## 回滚方案

- 将 `go.mod` 中 `myflowhub-sdk` 版本回退到 `v0.1.0` 并重新 `GOWORK=off go mod tidy`。
