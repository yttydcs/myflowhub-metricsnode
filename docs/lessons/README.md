# Lessons

Reusable troubleshooting notes for MyFlowHub-MetricsNode incidents.

## Entries

- [Android FGS and notification behavior](android-fgs-notification-behavior.md): Android 14 `Starting FGS with type camera` crashes and NotifyNode messages that only appear in the notification drawer or fail to show a heads-up banner.
- [MetricsNode reconnect and Android service restore](metricsnode-reconnect-android-keepalive.md): cross-platform disconnect recovery, stale `auth_snapshot.json` login state, NotifyNode resubscribe, metrics republish, Android `START_STICKY` null-intent restore, Android 13+ notification permission gating, and service refresh restarting NotifyNode pollers.
